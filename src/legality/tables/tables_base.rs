use crate::Species;

pub const LEGENDS: [usize; 46] = [
    Species::Mewtwo as usize,
    Species::Mew as usize,
    Species::Lugia as usize,
    Species::HoOh as usize,
    Species::Celebi as usize,
    Species::Kyogre as usize,
    Species::Groudon as usize,
    Species::Rayquaza as usize,
    Species::Jirachi as usize,
    Species::Deoxys as usize,
    Species::Dialga as usize,
    Species::Palkia as usize,
    Species::Giratina as usize,
    Species::Phione as usize,
    Species::Manaphy as usize,
    Species::Darkrai as usize,
    Species::Shaymin as usize,
    Species::Arceus as usize,
    Species::Victini as usize,
    Species::Reshiram as usize,
    Species::Zekrom as usize,
    Species::Kyurem as usize,
    Species::Keldeo as usize,
    Species::Meloetta as usize,
    Species::Genesect as usize,
    Species::Xerneas as usize,
    Species::Yveltal as usize,
    Species::Zygarde as usize,
    Species::Diancie as usize,
    Species::Hoopa as usize,
    Species::Volcanion as usize,
    Species::Cosmog as usize,
    Species::Cosmoem as usize,
    Species::Solgaleo as usize,
    Species::Lunala as usize,
    Species::Necrozma as usize,
    Species::Magearna as usize,
    Species::Marshadow as usize,
    Species::Zeraora as usize,
    Species::Meltan as usize,
    Species::Melmetal as usize,
    Species::Zacian as usize,
    Species::Zamazenta as usize,
    Species::Eternatus as usize,
    Species::Zarude as usize,
    Species::Calyrex as usize,
];

pub const SUB_LEGENDS: [usize; 47] = [
    Species::Articuno as usize,
    Species::Zapdos as usize,
    Species::Moltres as usize,
    Species::Raikou as usize,
    Species::Entei as usize,
    Species::Suicune as usize,
    Species::Regirock as usize,
    Species::Regice as usize,
    Species::Registeel as usize,
    Species::Latias as usize,
    Species::Latios as usize,
    Species::Uxie as usize,
    Species::Mesprit as usize,
    Species::Azelf as usize,
    Species::Heatran as usize,
    Species::Regigigas as usize,
    Species::Cresselia as usize,
    Species::Cobalion as usize,
    Species::Terrakion as usize,
    Species::Virizion as usize,
    Species::Tornadus as usize,
    Species::Thundurus as usize,
    Species::Landorus as usize,
    Species::TypeNull as usize,
    Species::Silvally as usize,
    Species::TapuKoko as usize,
    Species::TapuLele as usize,
    Species::TapuBulu as usize,
    Species::TapuFini as usize,
    Species::Nihilego as usize,
    Species::Buzzwole as usize,
    Species::Pheromosa as usize,
    Species::Xurkitree as usize,
    Species::Celesteela as usize,
    Species::Kartana as usize,
    Species::Guzzlord as usize,
    Species::Poipole as usize,
    Species::Naganadel as usize,
    Species::Stakataka as usize,
    Species::Blacephalon as usize,
    Species::Kubfu as usize,
    Species::Urshifu as usize,
    Species::Regieleki as usize,
    Species::Regidrago as usize,
    Species::Glastrier as usize,
    Species::Spectrier as usize,
    Species::Enamorus as usize,
];
